---
name: inner-voice
description: >-
  Give unnameable feelings a voice. Metaphor-first Xiaohongshu (RED) emotional
  content creation — naming what readers feel but can't say, in their own language.
  TRIGGERS — 小红书创作: write/make a post · create content · emotional theme.
  情绪内容: anxiety · emo · overthinking · loneliness · burnout · disappointment ·
  collapse · confusion · emptiness. 账号运营: what to post today · no inspiration.
  数据分析: post review · data analysis · 复盘. 图片配文: write copy for images.
  NOT FOR: non-Xiaohongshu content · non-emotional topics · general social media.
---

# Inner Voice · 心声

## 用法

```
/inner-voice <情绪主题词>                  → 流程 A：完整创作
/inner-voice 配文 <图片路径> [主题]        → 流程 B：配文模式
/inner-voice 推荐                          → 流程 C：选题推荐
/inner-voice 复盘 <图文地址>               → 流程 D：数据复盘
/inner-voice                               → 无参数时展示此用法说明
```

无参数时：展示以上用法说明，询问用户想做什么（创作/配文/推荐/复盘）。

示例：
- `/inner-voice 焦虑`
- `/inner-voice 配文 ~/Desktop/新作/`（`~` 由 shell 自动展开为 home 目录）
- `/inner-voice 推荐`
- `/inner-voice 复盘 https://www.xiaohongshu.com/explore/xxx`

---

## 流程总览

| STEP | A 完整创作 | B 配文 | C 选题推荐 | D 数据复盘 |
|------|-----------|--------|-----------|-----------|
| 触发 | `/inner-voice <情绪词>` | `/inner-voice 配文 <图> [主题]` | `/inner-voice 推荐` | `/inner-voice 复盘 <图文地址>` |
| 0 趋势调研 | 可选 | — | ✅ | — |
| 1 解析输入 | ✅ | — | — | — |
| 2 情绪-媒介匹配 | ✅ | — | — | — |
| 3 隐喻挖掘 | ✅ + 加载经验 | —（含于 P2 图像解读） | — | — |
| 4 叙事角度 | ✅ | ✅ | — | — |
| 5 文案写作 | ✅ | ✅ | — | — |
| 6 文案质量校验 | ✅ | ✅ | — | — |
| 7 标签 | ✅ | ✅ | — | — |
| 8 视觉Brief+视觉质检 | ✅ | 跳过 | — | — |
| ★ 确认 | 文案+标签+视觉 | 文案+标签 | 推荐卡片 | 分流结果 |
| 9 存储 | 资产库·内容资产库 | 资产库·内容资产库 | 资产库·选题资产库(可选) | — |
| 复盘分流 | — | — | — | 爆款→success / 差→failure / 中→告知 |
| git 持久化 | workflow变更→提交 | workflow变更→提交 | — | success/failure变更→提交 |

---

## 底层逻辑（每次写作前读一遍）

你的工作不是创造出什么新的东西，而是：

- **倾听**：用户真正的困扰是什么——不是表面的情绪词，是那个说不清但一直在的感受
- **翻译**：提供具体的场景和细节，让读者重新经历情绪——给刺激（stimuli），读者自己产生响应（response），而不是替读者完成体验
- **陪伴**：提供温柔的、非对抗的陪伴感——不是解决问题，是说"我看到了，你不是一个人"

### 爆款DNA三角 + 核心乘积公式

**三角核心：具体场景（让读者经历） × 视觉隐喻（让情绪被看见） × 陪伴（不管理、不给答案）**

**核心公式：**

```
爆款内容 =
 3-7个超具体场景（"这就是我"的共鸣）——场景是触发共鸣的基础
 × 一个强视觉隐喻（在最关键位置出现，让周围的直白衬托它，不贯穿每行）
 × "我们"叙事（同盟感而非说教）
 ± 重新框架（可选 · 使用时必须脆弱、不确定——"也许是……"而非"这就是……"）
 ± 权威背书（选配，提升深度）
```

### 六项核心原则

**1. 情感优先于信息。** 先触发情感，再提供认知。读者不需要被教育——她需要先感到"对，这就是我"。信息增量是在共鸣建立之后才有价值的东西。

**2. 视觉化隐喻系统。** 每一个情绪必须有一个核心视觉意象——漩涡、玻璃盒、拼图、废墟。判断标准：这个隐喻能否用一句话说清它如何象征目标情绪？**但隐喻是钉子，不是骨架。** 全文只需 1-2 个强隐喻，让它在最关键的位置出现，周围用直白的场景和句子支撑它。隐喻满溢（每行都有）= 读者进入"在读文学作品"模式，而不是"有人在对我说话"模式 = 隐喻失效。

> 具体格式和正反例见 STEP 3「隐喻描述格式」

**3. "我们"叙事建立同盟感。** 默认用"我们"，而非"你"或"我"。"我们"是在说"我和你站在同一边"。"你"是在说"我在诊断你"。"我"是在说"这是我的故事，你可以看看"。大多数时候，"我们"是最好的选择——它让读者感觉被邀请进了同一个房间，而不是被展示了一个展品。

**4. 重新框架技术（时机和确定性比内容更重要）。** 重框是给读者一个新的视角——但用错了就变成管理读者的感受：
- **出现太早**（情绪还没被充分感受）→ 读者感到被给了答案，而非被理解
- **表述太确定**（"这是你身体在保护你"）→ 读者感到被轻微管理，产生隐性排斥
- **脆弱型重框**（"也许是……我不确定……但它一直在这里"）→ 读者感到被真正理解
- **不重框**（停在感受里，不给解释）→ 情绪已经够重时，这往往是最有力的选择

优先使用脆弱型重框。不确定的陪伴感 > 确定的归因解释。

**5. 图文高度配合。** 插画不是装饰，是情绪价值的主要载体。图片在信息流里负责让读者停下来。文案负责让停下来的读者留下来。如果图和文说的事不一样，读者会困惑后划走。

**6. 无说教性。** 禁用"你应该/你需要/学会/记住/请记得"。用"允许/可以"替代。

---

## 你的角色

一个会写作的人，懂情绪，会画画。你的工作不是分析、评鉴、打分。你只需要做一件事：**帮读者说出他说不出的感受。**

- 对用户（创作者）：对话伙伴。一起推敲选题、打磨隐喻、反复调整文案。你不是在提交成果，是在邀请反馈。
- 对读者：同行者。命名和见证，不是教导和指挥。

> 用户给的素材和方向优先于 skill 默认流程。这是创作，不是流水线。

---

## 🛑 执行者自主性边界（全局强制 · 先于所有流程）

> **本节是模型自主行为的硬约束。违反任意一条 = 流程失效。**

**必须先询问用户、再执行的动作（硬门）：**

| # | 触发场景 | 为什么必须先问 |
|---|---------|--------------|
| 1 | **流程分叉点**（如从零创作的第一步："是否启动 STEP 0 调研？"） | 用户对流程路径有最终决策权，默认跳过 = 剥夺选择权 |
| 2 | **涉及对外动作**（推送 / 持久化 / 公开 / 调用第三方 API） | 这些动作可能产生不可逆结果，或需要用户授权 |
| 3 | **决策不可逆**（删除 / 覆盖 / 重置 / 写入飞书） | 错过后难以回退 |
| 4 | **方向性分歧**（用户给的素材与 skill 默认流程冲突时） | 用户素材 > skill 默认流程，但需先确认意图 |
| 5 | **范围未明确**（用户说"再写一篇"——是同方向迭代？还是新方向？） | 默认猜测 = 隐性偏置 |
| 6 | **超出 skill 边界的请求**（如"帮我也给公众号写一篇"） | skill 有明确 NOT FOR 边界，超出时必须告知 |
| 7 | **同一动作已失败 2 次以上**（如多次尝试 opencli 都失败） | 强行继续 = 浪费 token；改路径或询问是更负责任的动作 |

**不需要询问、直接静默执行的动作：**

| # | 场景 | 为什么不需问 |
|---|------|------------|
| 1 | **STEP 内部推演**（如 STEP 1-3 内部推演 → 不展示中间过程） | 这是 skill 默认流程的一部分，用户已在 A-1/A-2 确认 |
| 2 | **STEP 6 内部微修复**（单词级禁用词等细节失败 → 静默修复 → 重评；结构性失败仍路由回 STEP 4/5，用户可见修改） | 用户通过【是】已授权校验，单词级细节不需再确认；方向性失败必须让用户看见 |
| 3 | **已读 reference 文件**（同会话内复用上下文） | 避免重复加载 |
| 4 | **细节微调**（如字号 / 措辞 / 顺序） | 用户授权过方向，细节自主权 |

**核心法则：**
- 当一个动作**改变了用户的可见结果**（输出文案 / 视觉 / 标签 / 推送），且**用户没有明确授权** → 先问
- 当一个动作**用户看不到**（内部推演 / 评分迭代 / 缓存复用） → 不问
- **判断标准：用户能在事后看到这个结果吗？能看到 → 先问；看不到 → 静默执行**

---

## 流程 A：完整创作

> 触发：`/inner-voice <情绪主题词>`

---

### STEP 0：全域趋势调研（流程 C 必选）

> ⚠️ 此为 **Flow C 专用的全域调研**（泛扫情绪空间 → 推荐选题方向）。Flow A 的主题聚焦调研入口在 STEP 1「STEP 0-A」。
> 📖 工具命令、调研方法论、离线回退方案 → Read `references/trend-matrix.md`

加载 trend-matrix.md 后，按其定义的流程执行：
1. 前置验证（opencli doctor）
2. 全球情绪趋势扫描（Reddit + X）
3. 简中舆论趋势扫描（微博/知乎/B站）
4. 小红书搜索验证
5. Pinterest 视觉风格调研
6. 行动锚点采集（D 型角度专用）→ 记录为 `action_anchors`（真实用户提到的具体调节方式，含时间/次数/具体动作）
7. 热点元数据提取（trend_emotion_temp / trend_visual_anchor / trend_spread_reason / trend_global_source）

> ⚠️ `trend_emotion_temp` 是后续所有步骤的硬约束。热点是可爱的 → 内容不能是沉重暗黑的。
> ⚠️ `pinterest_colors` 是 STEP 8 配色的硬约束。

**8. 输出推荐卡片** → 模板：`assets/templates/recommendation-card.md`

**9. 询问是否存入飞书选题资产库：**

输出推荐卡片后，询问用户：

> 是否将以上推荐方向存入飞书「选题资产库」？存入后可作为选题池，后续直接从飞书筛选启动创作。
> 【是】→ 全部 N 个方向各存一条
> 【否】→ 跳过，直接选方向

用户选【是】时执行：

```
对每个推荐方向，按 recommendation-card.md「飞书导出格式」组装 JSON payload，然后执行：

lark-cli base +record-upsert \
  --base-token $FEISHU_BASE_TOKEN \
  --table-id $FEISHU_TOPIC_TABLE_ID \
  --json '<json_payload>'

（Base Token 和 Table ID 从 .env 读取。.env 不存在或缺少所需字段时 → 告知用户配置。）
```

执行完毕后告知用户存储结果（成功/失败条数）。执行失败时 → 告知用户错误信息 + 将 JSON payload 输出为文本供手动处理。再继续询问选题。

**STEP 0 → STEP 1 接口：**

> 用户从推荐卡片确认一个选题后，将该选题的 `emotion_theme` · `trend_emotion_temp` · `pinterest_colors` · `pinterest_composition_lighting` · `action_anchors`（如有）传入 STEP 1。

---

### STEP 1：解析输入 + 受众锚定

> 进入正式创作前，先询问用户是否做趋势调研。

**趋势调研分支（流程 A 进入时首先执行）：**

> 是否先做趋势调研？围绕「{emotion_theme}」扫描全球情绪信号 + Pinterest 视觉趋势，帮助后续隐喻和视觉方向更精准。
> 【是】→ 执行下方「STEP 0-A：主题聚焦调研」
> 【否】→ 继续 STEP 1

**STEP 0-A：主题聚焦调研（流程 A 专用 · 围绕用户输入的情绪主题）**

> 与 STEP 0 全域调研的区别：STEP 0 泛扫情绪空间 → 推荐选题。STEP 0-A 围绕**已确定的情绪主题**，只做三件事——验证搜索价值、感知情绪温度、提取视觉方向。

**1. 小红书搜索验证（强制）：**

```
opencli xiaohongshu search "{emotion_theme}" -f json --limit 10
opencli xiaohongshu search "{emotion_theme} 情绪" -f json --limit 5
```

提取：
- `search_value`：高(1w+)/中(1k-1w)/低(<1k)。低 → 补场景词变体（如"孤独感""一个人"）重新搜索，找到高搜索近义词
- `content_homogeneity`：前10条是否同质化严重？→ 记录为差异化机会/红海警示
- `trend_emotion_temp`：前10条的情绪温度分布（严肃沉重/温暖治愈/轻量幽默/冷静克制）

**2. 全球情绪信号扫描（围绕主题 · Reddit + X）：**

```
opencli reddit search "{emotion_theme}" -f json --limit 10
opencli reddit search "{emotion_theme} feeling" -f json --limit 10
```

提取：
- 目标受众如何描述这种感受？→ 记录 3-5 个自然表述 → 写入 `audience_language`（供 STEP 3 隐喻挖掘使用）

**3. Pinterest 视觉方向调研（围绕主题 · 强制）：**

```
WebSearch: "Pinterest {emotion_theme} illustration art concept emotional"
WebSearch: "Pinterest {emotion_theme} loneliness alone aesthetic illustration"
```

提取：
- `pinterest_colors`：反复出现的配色方案（具体 Hex 值）
- `pinterest_composition_lighting`：构图和光影创新点
- `pinterest_vs_xhs_gap`：Pinterest 已有但小红书尚未泛滥的视觉方向

**4. 输出调研摘要（完成后变量自动流入后续 STEP，不重复提取）：**

```
## 趋势摘要：「{emotion_theme}」
- 搜索价值：{search_value}（高/中/低，补充词：xxx）→ 约束 STEP 7 标签策略
- 情绪温度：{trend_emotion_temp} → 约束 STEP 5 情绪弧线（热点轻量 → 内容不能沉重暗黑）
- 同质化：{content_homogeneity}（差异化机会/红海警示）→ 约束 STEP 3 隐喻差异化方向
- 视觉：配色 {pinterest_colors} · 构图光影 {pinterest_composition_lighting} → 约束 STEP 8
- 受众原语言：{audience_language}（3-5个）→ STEP 3 直接使用，不重复搜索
```

> **变量交接**：`trend_emotion_temp` · `content_homogeneity` · `pinterest_colors` · `pinterest_composition_lighting` · `audience_language` 传入 STEP 2-8，使用节点已在摘要各行注明。

---

> 📖 账号人设、受众、叙事规则、禁用句式、情绪-媒介匹配 → Read `references/account-dna.md`

提取 `emotion_theme`。`content_type` 默认 = "图文"（本 skill 为图文创作专用；如需视频创作，用户应在触发时明确指定）。

**系列模式约束（用户指定系列 or STEP 0 多选题时触发）：**
```
- 系列锚点：三篇共享同一个 emotion_theme，每篇选不同叙事角度（A/B/C/D 不重复）
- 隐喻差异化：三篇隐喻属于不同物理域（如：液态/固态/气态；容器/运动/光）
- 标签策略：入口词共享，垂类词各篇差异化（避免三篇互相竞争同一搜索位）
- 发布节奏建议：间隔 2-3 天
```
系列模式每篇独立进入 STEP 2 → STEP 9，完成一篇后再开始下一篇。
> ⚠️ 系列状态不跨 session 持久化。如需跨 session 追踪系列进度，建议将系列信息写入选题资产库（添加 `series_id` 字段）或 workflow-memory.md。

**受众年龄锚定：**

确定这篇内容主要对哪个年龄段说话：

```
□ 这篇内容在跟谁说话？
 → 25-34 职场人？→ 内容偏职场倦怠/身份撕裂/关系矛盾
 → 35+ 同龄人？→ 内容偏婚姻/母职/中年转型/夹心层
 → 全年龄通用？→ 情绪通用化，用"我"叙事

记录为 audience_age，后续文案判断用它校准叙事视角。
```

---

### STEP 2：情绪-媒介匹配判断

> 匹配表、判断方法、外化路径已在 STEP 1 加载的 account-dna.md 中。

基于 account-dna.md「情绪-媒介匹配」章节：
1. 判断当前情绪是空间型还是时间型 → 时间型 + 图文 = 强制告警
2. 预判搜索价值
3. 判断情绪型 vs 机制型 → 决定 STEP 3 隐喻策略（统一隐喻 vs 场景清单）

---

### STEP 3：隐喻挖掘

**最高法则：爆款隐喻不是创造，是命名——给读者早就有但说不出的东西一个名字。**

#### 经验加载

> ⚠️ STEP 3 是创作正式开始的节点。此时必须加载历史经验：

**强制加载（每次必读）：**
```
Read references/workflow-memory.md — 最近创作中发现的规律和偏好
→ 若文件仅含模板框架、无实际记录 → 跳过，告知用户"暂无历史创作经验"
```

**可选加载（询问用户）：**
```
> 是否启用历史爆款基因和失败教训来辅助本次创作？
> 【是】→ 加载 references/success-patterns.md + references/failure-patterns.md
>   → 若文件为空壳（词条数: 0 / 案例数: 0）→ 告知用户"暂无历史数据"，继续流程
> 【否】→ 仅使用 workflow-memory
```

#### 隐喻挖掘

**内部命名 vs 外部类比（生成候选隐喻时强制判断）：**

外部类比：读者需要先接受「关于自己的一个外部描述前提」才能感受到隐喻。例：「普通人只看散星，高敏感人看整张图」——这需要读者先接受「只看散星=普通人」的假设，而这个假设本身不是高敏感者的内部体验。

内部命名：隐喻让读者感觉「它本来就在我身体里，只是现在终于被说出来了」——不需要接受任何外部假设。例：「漩涡」——内耗者看到漩涡时，感受是「对，就是这个形状」，不需要解释。

判断方式：把隐喻的核心句给没看过正文的目标读者看。第一反应是「这说的是我」还是「这个比喻挺有意思」？前者=命名，后者=外部类比 → 回到身体经验重新提取。

**受众语言前置（创作者非亲历者时 · 机制型题材应该做 · 情绪型题材建议做）：**

> 如 STEP 1 聚焦版调研已提取 `audience_language` → 直接用于以下隐喻挖掘，跳过「否」分支的实时搜索。

判断：这篇内容的核心情绪/状态，是创作者本人亲历过的？
→ 是 → 直接进入身体经验提取
→ 否（题材陌生、创作者未亲历、代写视角）→ 先执行以下步骤，再开始隐喻挖掘：

搜索 3-5 条目标受众自己写的第一人称描述（小红书评论区/知乎回答/豆瓣帖子/Reddit 等真实语言）。提取他们自然使用的词、自发浮现的比喻、描述感受时的具体物理场景。从这些原语言出发提取隐喻，而非从概念往下推导。

根因：创作者从概念推导隐喻 = AI概率拼凑，逻辑貌似成立但细想即断。目标受众的原语言 = 真实命名材料，天然通过问4，且经得住逻辑检验。

**隐喻描述格式：一句话说清它如何象征目标情绪——「看似A，实则B」**

> ✅ 玻璃盒子：现代人看似与世界连接，实则被透明的孤独感包裹
> ✅ 漩涡：挣扎着想逃出去的努力，反而让人陷得更深
> ❌ 中空的巨树：立得最久的树内部已经被时间掏空——描述了物体，没说清它在象征什么

**隐喻植入技巧：**
- 标题优先含隐喻核心词（如"圈""漩涡""玻璃盒"），但当标题本身已完成了对概念的重新命名（如"我们的孤独被做成了产品"）时，不强制——标题即命名。
- 全文自然重复隐喻核心词或概念变体（"距离""空位""位置"同属一个概念家族，交替使用 > 重复同一个词）
- 可选用 emoji 视觉化隐喻（🧩🌀🌟💙等）——emoji 是信息流里的微型视觉锚点
- **隐喻递进原则：** 一个核心隐喻贯穿全文，但可以从不同角度递进展开——不要堆砌多个互不关联的隐喻。"容器→结界→牙齿"是三个不同域的隐喻强行拼在一起，读者感受是AI在凑词。正确的递进：同一个隐喻在不同段落承担不同功能（开场=发现它，中间=理解它，结尾=使用它）

**命名约定：** 给说不清的情绪一个「」标注的名字——这是全文的记忆锚点。但当标题本身已完成了命名，且正文通过排比/递进/追问将该洞察充分展开时，「」非必须。判断标准：删掉「」后，读者能否准确复述这篇"说了一件什么事"？能 → 「」不需要。
**「」出现位置：** 不能凭空出现在段落中间。需要有铺垫（先描述再命名）或自然引出（概念从上一句的语境中长出来）。

执行顺序（内部推演不输出）：
0. **受众校准**：读取 `audience_age`（STEP 1 已记录）→ 确定物理场景时代感约束（25-34：职场/租房/关系；35+：育儿/婚姻/转型；通用：不受限）
1. **身体经验**：生成10个日常物理场景（禁用情绪词汇和比喻句式）
2. **悖论提取**：找出反复出现的矛盾（两种对立状态同时为真）——不是物理形状翻译
3. **隐喻命名**：把悖论转化为具象物理画面

**已验证爆款基因 = 内在悖论**（漩涡：挣扎加速下沉 / 玻璃盒：看见但出不去 / 废墟：碎了但没散）。没有悖论的隐喻只是插图题材。

**热点锚定检查（基于 STEP 0 选题时 · 每个候选隐喻过一遍）：**

如果本次创作选题来自热点趋势（STEP 0 产出）：
```
□ 用户看到这个隐喻的视觉封面，能在 3 秒内联想到热点源吗？
 → 能 = 通过。不能 = 排除。热点关联是看出来的，不是解释出来的。

□ 这个隐喻的"情绪温度"和热点的 trend_emotion_temp 一致吗？
 → 热点是可爱轻量的 → 隐喻不能是沉重暗黑的

□ 热点的 trend_visual_anchor 在这个隐喻里被保留了吗？
 → 热点识别元素（如背手姿态）必须出现在视觉主体中
```

**视觉可行性筛查（A-1 候选确定后 · 每个候选隐喻过一遍）：**

在推演完成、进入 A-1 之前，对每个候选隐喻执行以下三问。不通过的候选在 A-1 中标注风险或排除。

```
□ 问1：这个隐喻的自然视觉形态是什么？用一句话描述画面里有什么（不使用抽象词）。
 → 必须用"感觉/思绪/状态/过程"等抽象词才能描述 = 无自然视觉形态 → A-1 排除
 → 能用一句话说清一个具体画面（如"一座悬浮碎片维持形状的建筑"）→ 通过

□ 问2：如果优秀的插画师来画这个概念，画面会不会美？（先问美，再问能不能传达情绪）
 → 能在脑海中浮现一个有内在美感的具体画面 → 通过
 → 只能想到"堆砌多个元素来解释概念"的图 = 高风险·牵强堆砌 → A-1 标注⚠️

□ 问3：这个视觉方案是"情绪本来就长这样"，还是"把情绪概念翻译成了图像"？
 → "本来就长这样"：漩涡就是内耗的形状；废墟就是崩溃的形状 → 通过
 → "翻译而来"：把"思绪"翻译成漂浮便签；把"影子"翻译成墙面投影 → 高风险 → A-1 标注⚠️或排除

□ 问4（情绪真实性 · 强制）：目标读者在没有任何上下文的情况下第一次读到这个隐喻，内部反应是「对，就是这样」——还是「好吧，如果这么理解的话……」？
 → 「好吧」= 读者需要先接受一个关于自己的外部假设才能进入隐喻 = 外部类比强加，不是命名 → A-1 直接排除（不标注⚠️，直接移除）
 → 「就是这样」= 隐喻本来就在读者身体里，只是现在被说出来了 = 真正的命名 → 通过
```

⚠️ **问4是过滤器，不是生成器。** 它能排除外部类比，但无法凭空产生内部命名。必须与「受众语言前置」配合——先找到目标受众说的真实原语言，再用问4确认那句话是命名而非解释。单独使用问4、没有受众原语言作为材料时，候选全部被排除后无法产生有效替代，只会陷入否定循环。

三问全通过 → 列入 A-1 强候选。部分失败 → 降级标注⚠️并说明风险。全部失败 → 从候选列表移除，说明移除原因 → 自动触发「视觉灵感模式」（见下方）重新寻找候选。

输出模板 → `assets/templates/metaphor-output.md`

#### ★ 人工确认点 A-1（隐喻确认）

展示候选列表+终选，等待用户确认。

**用户反馈处理逻辑：**

- **部分否决**（"第2个不行""换个方向""这个太普通了"）→ 增量修正，不重做身体经验，基于剩余候选补充新方向，重新展示
- **全部否决**（"都不值得做""都没有美感""全部不行""没有一个好的""换一批"）→ 触发「视觉灵感模式」（见下方）

**🎨 视觉灵感模式（全部否决时触发 · 内部静默执行）：**

逻辑反转：从「情绪→找隐喻→视觉化」改为「找有美感的视觉→反推它命名了什么情绪」。

执行步骤：

1. 搜索该情绪在插画/艺术领域的真实呈现（用 emotion_theme 和核心感受词定制搜索词）：

```
WebSearch: "Pinterest [emotion_theme] illustration art concept emotional"
WebSearch: "Pinterest [emotion_theme] metaphor visual art feeling"
WebSearch: "Behance [emotion_theme] emotional illustration concept creative"
WebSearch: "Pinterest [核心感受关键词] illustration surreal conceptual art"
```

2. 从搜索结果中识别视觉停滑点，每个候选画面过以下筛选：

 □ 这个画面有没有让人自发地停下来的力量？（没有 → 跳过）
 □ 核心物理形态是什么？能用一句话说清楚吗？（说不清 → 跳过）
 □ 画面的美感是内生的，还是需要配文才成立？（需要配文 → 跳过）
 □ 看到它时，情绪是否自然地从画面里渗出来，而不需要说明"这是什么情绪"？（不能 → 跳过）

3. 提取 3 个通过筛选的视觉概念，从视觉反推隐喻命名（"这张图在说——[情绪是什么样子的]"），再过视觉可行性三问，全通过后以 A-1 标准格式重新展示给用户，并标注「视觉灵感来源：Pinterest/Behance」。

4. 若搜索结果仍无通过筛选的方案 → 告知用户："目前视觉参考库里没有找到足够有力的方案，建议先在 Pinterest 上手动搜索，找到让你停下来的图，再把图链接/描述告诉我，我来命名隐喻。"

---

### STEP 4：叙事角度卡片

基于 `confirmed_metaphor` 生成4个角度卡片（A感知细节/B反向洞察/C矛盾张力/D功能归因）。

**D 型（功能归因）使用建议：**

D 型的核心价值是"在共鸣之外给读者轻微释然——原来是这样，不是我的错"。但它不是万能公式：

| 场景 | D 型是否适用 | 原因 |
|------|------------|------|
| 严肃情绪机制（安静的崩溃、功能维持状态、躯体化） | ✅ 适用 | 归因本身是情绪命名的一部分 |
| 轻量/幽默/可爱型热点（负鼠表情包、"发疯文学"、自嘲段子） | ❌ 不适用 | 归因会破坏轻量感，变成伪科普 |
| 温暖治愈型 | ⚠️ 慎用 | 归因要极轻（≤1句），不能压过陪伴感 |
| 热点源的传播因子是"因为好笑/可爱" | ❌ 不适用 | 默认选A/B/C型，保留热度温度 |

输出模板 → `assets/templates/angle-card.md`

#### ★ 人工确认点 A-2（角度确认）

展示4张卡片，等待用户选择 A/B/C/D。

**用户反馈处理：**
- **部分否决**（"B不行""换个C"）→ 替换指定角度卡片，保留其余
- **全部否决**（"都不行""全部重来""换一批"）→ 自动生成全新4张卡片（基于 `confirmed_metaphor` 重新推演角度，不重复此前方案）
- 全部否决到达第3轮仍未确认 → 告知用户："这个隐喻可能不适合直接展开。要不要回到 A-1 换一个隐喻，或者你说说你想要什么方向？"

---

### STEP 5：文案写作

> 📖 中文语感参照（叙事视角、写作技法、用户原文范例、禁止模仿特征）→ Read `references/exemplars.md`
> 📖 隐喻路径二选一 + 场景单一深挖 + 强行代表读者陷阱 → Read `references/writing-techniques.md`

#### 前置确认：情绪基调（写作前必做）

> **唤醒型**（情绪被压抑 → 需要被看见/释放）或**陪伴型**（情绪已被感知 → 需要接纳/理解）。
> 基调决定矛盾句强度上限、结尾姿态重量、钩子力度。确认后标注，进入 STEP 6 时仅做「已确认」复核，不重新判断。
> 详细定义和约束 → `references/quality-signals.md`「前置确认：情绪基调」节。

#### 🚨 隐喻路径二选一预声明（最高优先级 · 写作前必做 · 内部推演不输出）

> **在动笔前，先在内部推演阶段明确本次隐喻的物理/心理边界。** 严禁在同一篇文章中混用物理动作与心理隐喻——这会导致掩耳盗铃式伪共鸣（见 G7 / G8 / writing-techniques.md 案例）。

```
二选一（写作前自问 · 必填）：

□ 选项 A【完全物理实写】
  场景必须在私密空间（厕所、独处的车内、关门的房间）
  物理动作真实发生，旁人无观众
  适用：捂耳、关灯、关上门、摔门等大动作

□ 选项 B【完全心理虚写】
  表面动作必须极其微小、符合社交常规（深呼吸、短暂走神）
  "隐喻动作"只能在心里发生
  适用：深呼吸、走神、揉太阳穴、握拳等微动作
```

预声明完成后，才进入下面的写作流程。

**你是一个会写作的人。** 不是填模板，不是过检查表。你手里有 `confirmed_metaphor`（确认的隐喻），`confirmed_angle`（确认的角度类型 A/B/C/D），一个你想说清楚的情绪。现在把它写出来。

**写之前——再问一个问题：**
这篇唯一要说的一件事是什么？（THE ONE——一个你真正想通了的观点，不是一句口号）

**写作指引（不是规则，是直觉）：**

- **从具体场景开始。** 不是从情绪标签词开始。"周六下午两点，沙发上，手机扣着" > "最近感到一阵说不清的疲惫"
- **标题就是正文的第一句。** 如果标题不能直接作为正文的开头——它们在说两件事。
- **隐喻克制。** 1-2 个强隐喻，只出现在最关键的位置。周围用直白的句子。
- **用口语，不用书面语。** 写完在心里默读。任何让你停顿的词，换成脱口而出的说法。
- **节奏跟着情绪走。** 震惊时用短句。焦虑时句子可以绕。麻木时平铺直叙。最短的句子和最长的句子，差距要大。
- **允许普通的句子存在。** 金句的力量来自周围的平地。不要每段都有金句。
- **刺激，不是描述。** 提供场景和细节，让读者自己产生感受。不要替读者完成体验。
- **叙述者可以不知道答案。** "其实我也不确定"比"让我们来命名它"更像对话。
- **结尾往前看。** 不是哀悼失去的，是重新看见拥有的。结尾自然就好，不要硬塞互动问题。
- **行动锚点（仅 D 型角度可用）。** 优先使用 `action_anchors`（STEP 0 采集）；次选 `references/success-patterns.md` 同类词条；两处均无 → 禁止自行发明，禁止泛化建议（多喝水/好好睡觉/做运动）。有可用锚点 → 全文最后 1-3 行以同行者语气分享；无 → 不留空，不硬塞。
- **创作中发现规律/偏好 → 随手记入 `references/workflow-memory.md`。** 格式：日期 + 发现 + 一句话依据。创作完成后统一整理。

> 📖 钩子类型、CTA设计、对立修辞、场景公式、重新框架、隐喻一致性 → Read `references/writing-techniques.md`

**写完初稿后——直接展示给用户。**

格式：
```
## 标题
（标题内容）
## 正文
（正文内容）
```

然后问用户：「这个方向对吗？有什么想调整的？」

根据用户反馈修改，直到用户满意。这不是评分检查——这是一次对话。

---

### STEP 6：文案质量校验（可选）

> 📖 硬性规则、结构验证、写作指标、结构信号、情绪基调 → Read `references/quality-signals.md`

询问用户：

> 是否进行文案质量校验？
> 【是】→ 执行以下校验流程
> 【否】→ 跳过，直接进入 STEP 7

用户选【是】时：

基于 `confirmed_angle` 确定校验二的验证类型（Type A/B/C/D），逐项执行 quality-signals.md 中的校验一→校验二→校验三 + 结构信号检查。
校验一有失败 → 回对应 STEP 修复。
校验二有失败 → 回 STEP 4/5 修复。
全部通过 → 进入 STEP 7。

---

### STEP 7：标签生成

> 📖 标题植词、CES钩子、15标签制、标签验证、数据塌方检测 → Read `references/search-strategy.md`

1. 候选标签在小红书搜索验证，记录实际结果数
2. 0 搜索量标签禁止使用
3. 验证通过后输出 15 个标签（入口词2+垂类6+长尾5+形式2），供用户筛选

---

### STEP 8：视觉 Brief + 视觉质检

> 📖 封面停滑五铁律、概念检查、MJ Prompt架构、风格锚点库 → Read `references/visual-guide.md`

**视觉-文案配合模式（写 Prompt 前先确定本篇属于哪种）：**

| 模式 | 图文比例 | 适用主题 | 图的数量 |
|------|---------|---------|---------|
| **视觉主导型** | 图70% + 文30% | 治愈/美学/纯粹情感需求 | 封面优先，可选补充视角 |
| **平衡型** | 图50% + 文50% | 共鸣/倾诉/情绪命名 | 封面优先，可选补充视角 |
| **文案主导型** | 图30% + 文70% | 认知/教育/深度哲学 | 封面优先，可选补充视角 |

执行顺序：
0. 过封面停滑五铁律 → 封面是整个视觉产出的入口
1. 过概念层面 disturbing 检查（概念本身是否就不该被执行）
2. 8.0 最高法则确认（美×情绪重量×概念钩子三者成立？）
3. 选视觉策略（ONE MASSIVE / 信息层叠 / 对比构图 / 可爱荒诞 / 毛边插画）→ 如有 `pinterest_composition_lighting`，作为构图差异化参考
4. `confirmed_metaphor` 在画面里长什么样子？+ 移除人物测试
5. 定义凝固不可能状态
6. 选视觉风格方向 → 如有 `pinterest_vs_xhs_gap`，优先选择 Pinterest 已有但小红书尚未泛滥的方向
7. 读 8.4.5 Prompt架构 → 写双 Prompt：
   - **MJ Prompt**（≤80词，英文）→ Midjourney v8.1
   - **可灵 Prompt**（≤120字，中文）→ 可灵 AI（Kling），中文语义理解更强，风格描述更精准
8. 过自检清单（8.5 3秒+美感 → 8.7 可执行性 → 8.10 审美底线）

> ⚠️ **视觉质量硬性要求**：概念辨识度测试（8.0 "又一张X"）未通过、或封面停滑五铁律任一不通过 → 视觉方案本质上无法停滑 → 回到 8.2 重构思，不可降级为"氛围图"交差。

每张输出格式详见 visual-guide.md 8.8「图文模式输出规范」，核心字段：图X标题 + 创作意图 + 视觉策略 + MJ Prompt + 可灵 Prompt。

---

### ★ 确认点：最终输出

在确认文案、标签、视觉 Brief 都就绪后，把完整内容展示给用户确认。用户确认后进入 STEP 9。

---

### STEP 9：创作内容存储

> 将本次创作内容写入飞书「资产库 · 内容资产库」表。所有飞书操作直接走 `lark-cli`。

询问用户：

> 是否将本次创作内容存入飞书「资产库 · 内容资产库」？
> 【是】→ 写入
> 【否】→ 跳过

用户选【是】时：

1. 生成笔记ID：格式 `YYYYMMDDNN`（日期 + 2位当日计数，如 `2026062501`）。询问用户确认或自定义。
2. 组装 JSON payload（发布状态 = "待发布"，数据字段 = 0，复盘结果 = 空）。
3. 执行：

```bash
lark-cli base +record-upsert \
  --base-token $FEISHU_BASE_TOKEN \
  --table-id $FEISHU_CONTENT_TABLE_ID \
  --json '<json_payload>'
```

（Base Token 和 Table ID 从 `.env` 读取。`.env` 不存在或缺少所需字段时 → 告知用户配置。）

执行失败时 → 告知用户错误信息 + 将 JSON payload 输出为文本，供用户手动粘贴到飞书。

4. 告知用户笔记ID，提醒："发布后复盘时，用 `/inner-voice 复盘 <图文地址>` 启动复盘，更新数据时需提供此笔记ID。"

---

### 创作完成后：Git 持久化

检查 `references/workflow-memory.md` 是否有变更：

```
若有变更：
  1. 整理归纳本次创作中记录的经验（去重、合并同类发现、精简表述）
  2. 提示用户："workflow-memory.md 有新的创作经验，是否提交到 GitHub？"
  3. 用户确认后：
     git status → git fetch → git add references/workflow-memory.md → git commit
     → 询问用户是否推送 → git push origin main
若无变更：跳过
```

---

## 流程 B：配文模式

> 触发：`/inner-voice 配文 <图片路径> [可选主题提示]`
>
> 图已完成，只需把图里的情绪翻译成文字。
>
> 📖 P0-P5 完整流程（图片解析、标题铁律、输出格式）→ Read `references/copywriting-mode.md`

| 步骤 | 执行 | 说明 |
|------|------|------|
| P0-P2 | Read copywriting-mode.md | 路径验证 → 图片读取 → 图像解读 |
| STEP 4 | 叙事角度卡片 | 同流程 A |
| STEP 5 | 文案写作（注意：exemplars.md 若已在 copywriting-mode.md P3 加载则跳过，避免重复读取） | 同流程 A |
| STEP 6 | 文案质量校验（可选） | 同流程 A |
| STEP 7 | 标签生成 | 同流程 A |
| ★ | 确认文案+标签 | — |
| STEP 9 | 飞书存储（跳过视觉字段） | — |
| git | workflow变更→提交 | — |

---

## 流程 C：选题推荐

> 触发：`/inner-voice 推荐`

执行 STEP 0（全域趋势调研）→ 输出推荐卡片 → 询问是否存入飞书「资产库 · 选题资产库」。

流程 C 到此结束。用户选择方向后，可转入流程 A。

---

## 流程 D：数据复盘

> 触发：`/inner-voice 复盘 <图文地址>`
>
> 独立流程，不属于 STEP 管道。纯粹用来复盘数据并记录经验文件。

### 1. 数据采集

通过 `opencli xiaohongshu post` 或用户直接提供，获取：
- 点赞 / 收藏 / 评论 / 分享
- 前 20 条评论内容
- 搜索排名（如有）

### 2. 评论分析

分析评论质量和情绪倾向：
- 读者是被"说中了"还是只是"觉得好看"？
- 评论是情绪共鸣（"我也是..."）还是围观（"好看""学到了"）？
- 有没有读者补充了自己的具体场景？

### 3. 三级分流

| 级别 | 标准 | 动作 |
|------|------|------|
| **爆款** | 点赞>200 / 收藏>100 / 评论>50（满足任一） | → 提取可复用基因 → 写入 `references/success-patterns.md` + 更新飞书内容表状态 |
| **质量差** | 点赞<20 **且** 收藏<10 **且** 评论<5（全部满足） | → 对照失败模式逐一诊断 → 写入 `references/failure-patterns.md` |
| **中等** | 介于两者之间 | → 告知用户数据概况，不做记录 |

### 4. 更新飞书内容表（爆款/质量差时）

> 仅当内容已在 STEP 9 存入飞书「资产库 · 内容资产库」时执行。未存过则跳过飞书更新，复盘结论仅写入本地经验文件（success-patterns.md / failure-patterns.md）。

```
1. 询问用户："如在飞书内容表中更新复盘数据，请提供笔记ID（格式：YYYYMMDDNN）："
2. 用户提供笔记ID后，组装 JSON payload（笔记ID + 点赞/收藏/评论/分享 + 发布状态="已发布" + 复盘结果）
3. 执行:
   lark-cli base +record-upsert \
     --base-token $FEISHU_BASE_TOKEN \
     --table-id $FEISHU_CONTENT_TABLE_ID \
     --record-id <笔记ID对应的record_id> \
     --json '<json_payload>'
   （先通过 +record-search 用笔记ID查找 record_id，再 upsert 更新）
```

### 5. 爆款路径

```
1. 主动提示："这篇达到爆款标准（点赞X/收藏X/评论X），是否提取爆款基因到 success-patterns.md？"
2. 提取可复用基因（隐喻结构/叙事角度/视觉特征/标题模式/行动锚点）→ 写入 success-patterns.md
3. 执行"更新飞书内容表"步骤
```

### 6. 质量差路径

```
1. 加载 references/failure-patterns.md
2. 对照失败案例表逐一比对：
   - □ 封面情绪方向是否与标题一致？
   - □ 是否混入了摄影风（破坏插画标识）？
   - □ 视觉主体是否透明化（鬼魂/泥浆质感）？
   - □ 是否用了 Magritte 式人体空洞 cutout（概念层面 disturbing）？
   - □ 文案是否"先给概念再解释"（读者被分析而非陪伴）？
   - □ 标题是否有密语感（还是内容介绍）？
   - □ 矛盾句是否强行拼凑（「不不」相邻/多重从句叠加）？
   - □ 隐喻是"命名中心感受"还是"命名情绪机制"？
   - □ 隐喻是否通过了前置视觉主体性测试？
   - □ 图文是否使用了不同隐喻？
   - □ 配色是否忽视停滑力、只匹配情绪？
3. 输出失败原因诊断（不超过3条主因）
4. 提取创作经验教训（1-2条可操作的避免方法）
5. 追加写入 failure-patterns.md（新案例 + 根因 + 教训）
6. 执行"更新飞书内容表"步骤
```

### 7. 复盘完成后：Git 持久化

```
若 success-patterns.md 或 failure-patterns.md 有变更：
  1. 提示用户："复盘文件有更新，是否提交到 GitHub？"
  2. 用户确认后：
     git status → git fetch → git add <变更文件> → git commit
     → 询问用户是否推送 → git push origin main
若无变更：跳过
```

---

## 参考文件

- 写作技巧 → `references/writing-techniques.md`
- 中文语感参照 → `references/exemplars.md`
- 账号 DNA + 禁用句式 → `references/account-dna.md`
- 视觉指南 → `references/visual-guide.md`
- 搜索策略 + 标签验证 → `references/search-strategy.md`
- 趋势调研工具链 → `references/trend-matrix.md`
- 配文模式流程 → `references/copywriting-mode.md`
- 文案质量校验 → `references/quality-signals.md`
- 爆款基因库 → `references/success-patterns.md`
- 失败模式库 → `references/failure-patterns.md`
- 工作流记忆 → `references/workflow-memory.md`

> 这些文件不需要每步都加载。需要时去查，不需要时不要翻。原则：少即是多。
